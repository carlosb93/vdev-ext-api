const ExtractionDAO = {
  create (extraction) {
    return new Promise((resolve, reject) => {
      return resolve('Se guardaron correctamente los datos');
    });
  }
}

module.exports = ExtractionDAO
